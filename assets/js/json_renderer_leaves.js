// Generated by CoffeeScript 1.6.1
var JSONRenderer;

JSONRenderer = {
  render_json: function(json) {
    var result;
    result = this.render_json_helper(json);
    return "<div class='json_renderer'>" + result + "</div>";
  },
  render_json_helper: function(json) {
    var items, items_rend, result,
      _this = this;
    return result = json instanceof Array ? (items = _.pairs(json).map(function(pair) {
      return _this.render_pair(pair[0], pair[1]);
    }), items_rend = items.length === 0 ? "<span class='empty'>empty</span>" : _(items).join(""), "<div class='array_items'>          " + items_rend + "       </div>") : json == null ? "<span class='null_val'>null</span>" : typeof json === "object" ? (items = (_.pairs(json).map(function(pair) {
      return _this.render_pair(pair[0], pair[1]);
    })).join(""), items_rend = items.length === 0 ? "<span class='empty'>empty</span>" : _(items).join(""), "<div class='hash_items'>         " + items_rend + "       </div>") : "" + json;
  },
  render_pair: function(key, value) {
    if (value instanceof Array) {
      return "<div class='array path_" + key + "'><div class='array_name'>" + key + " :</div>" + (this.render_json_helper(value)) + "</div>";
    } else if (typeof value === "object") {
      return "<div class='hash path_" + key + "'><div class='object_name'>" + key + " :</div>" + (this.render_json_helper(value)) + "</div>";
    } else {
      return "<div class='atom path_" + key + "'><span class='atom_name'>" + key + " :</span><span class='atom_value'>" + (this.render_json_helper(value)) + "</span></div>";
    }
  }
};
